import { ChapterAccessGuardGuard } from './chapter-access-guard.guard';

describe('ChapterAccessGuardGuard', () => {
  it('should be defined', () => {
    expect(new ChapterAccessGuardGuard()).toBeDefined();
  });
});
