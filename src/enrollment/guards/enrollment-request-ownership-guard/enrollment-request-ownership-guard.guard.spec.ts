import { EnrollmentRequestOwnershipGuardGuard } from './enrollment-request-ownership-guard.guard';

describe('EnrollmentRequestOwnershipGuardGuard', () => {
  it('should be defined', () => {
    expect(new EnrollmentRequestOwnershipGuardGuard()).toBeDefined();
  });
});
